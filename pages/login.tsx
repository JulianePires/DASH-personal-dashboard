import { NextPage } from "next";

const Login: NextPage = () => {
  return (
    <div className="bg-gradient-to-r from-dash-background-dark to-dash-gray-darker flex flex-col items-center justify-center h-screen w-screen"></div>
  );
};

export default Login;
